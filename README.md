Выводил данные пользователя на страницу через прямое обращение к свойствам user.name, user.age и так далее, но забыл оформить это в виде отдельной карточки профиля с фоном и отступами, поэтому потом обернул вывод в div с классом и стилями, чтобы карточка выглядела аккуратно

При обращении к вложенным свойствам товара product.specs.cpu сначала думал, что specs — это просто строка, но оказалось, что это объект, и если бы его не было, код упал бы с ошибкой, поэтому в теории нужно проверять существование, но в моём примере объект гарантированно есть, и я просто вывел характеристики списком через шаблонную строку

В задаче с массивом пользователей нужно было отметить активных пользователей отдельным классом, я сначала проверял условие if (user.active === true), но можно было просто if (user.active), а при генерации HTML через map забыл про метод join(), из-за чего между карточками вставлялись запятые, потом исправил

Не понимал смысла деструктуризации, ведь можно было просто написать user.name, user.email и user.role, но поэкспериментировав, понял, что это удобно, когда нужно достать несколько свойств из объекта одной строкой, и оставил в коде старый способ в комментарии для сравнения, причём сначала забыл, что имена переменных при деструктуризации должны совпадать с именами свойств, иначе получишь undefined

Раньше всегда склеивал строки через плюс, а по заданию нужно было использовать шаблонные строки с обратными кавычками, долго привыкал, и в начале ошибался, когда вставлял переменную — писал $value вместо ${value}, потом понял и исправил, а также убедился, что внутри строки можно делать переносы без лишних пробелов

Самое сложное в задаче с объектом заказа — посчитать итоговую сумму, потому что внутри order.items лежит массив, и нужно пройтись по нему, сложив price * count, вынес это в отдельную функцию calcTotal, но забыл обработать случай, когда массив пустой, поэтому добавил проверку на длину массива, а при динамическом построении таблицы использовал reduce внутри map, что вызвало путаницу в скобках, но в итоге разобрался