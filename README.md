Долго думал, как сделать калькулятор с отдельными функциями для каждой операции, написал add, sub, mul, div, но в div забыл проверку на ноль, из-за чего при делении на ноль программа вылетала или выдавала Infinity, потом добавил условие if (b === 0) return Ошибка: деление на ноль, чтобы возвращать понятное сообщение

При генерации таблицы умножения сначала не очищал контейнер перед новым выводом, из-за чего при повторном нажатии на кнопку результат просто дописывался снизу, потом понял, что нужно каждый раз делать innerHTML = пустая строка, и добавил очистку в начало обработчика

Не понимал, как создавать карточки товаров через JS, не перебирая HTML-строки вручную, сначала пытался использовать document.createElement, но забыл добавить классы для стилизации, потом нашёл способ через шаблонные строки и innerHTML, что оказалось проще и нагляднее

При добавлении чисел в массив для расчёта суммы и среднего не проверял, пустой ли input, из-за этого пустые строки попадали в массив как NaN и ломали все расчёты, добавил проверку if (num === пустая строка или isNaN(num)) return, чтобы отсеивать некорректный ввод

Для проверки пароля не знал, как проверить наличие хотя бы одной цифры и хотя бы одной буквы, думал, что нужно циклом проходить по всей строке, но потом вспомнил про регулярные выражения /\\d/ и /[a-zA-Z]/, что сильно упростило код, и добавил проверку длины пароля до проверки на цифры и буквы

В задаче с областью видимости сделал пример, где глобальная переменная менялась внутри функции, а локальная была недоступна снаружи, и долго не понимал, почему после вызова функции глобальная переменная изменилась, а локальная выдаёт ошибку в консоли, после чего закомментировал ошибочную строку и написал пояснение