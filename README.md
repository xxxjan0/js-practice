Сначала не понимал, как взять значение из input, написал document.querySelector(#nameInput), но забыл value, потом понял, что нужен value.trim(), и изначально выводил Привет, undefined, когда поле пустое, поэтому добавил проверку на пустую строку

Не переводил строку из input в число, использовал value, а надо было parseFloat(), при вводе пустого поля получал NaN, потом добавил проверку isNaN(), чтобы выводить сообщение об ошибке вместо некорректного результата

Сначала проверял только age < 18, а потом забыл про отрицательные числа, пришлось добавить условие if (age < 0), чтобы отсекать невозможные значения, и уже после этого обрабатывать возрастные категории

Пытался сделать проверку формы через required в HTML, но по заданию нужна была проверка через JS, сначала не знал, как собрать ошибки в одну строку, поэтому сделал переменную error и добавлял в неё текст для каждого незаполненного поля, а в конце выводил всё сразу

Перепутал порядок условий при проверке цены, сначала написал if (price < 1000) раньше, чем price === 0, из-за этого ноль попадал в категорию Дешёвый товар, пришлось переставить условия: сначала проверка на отрицательное значение, потом на ноль, потом на диапазон до 1000, и так далее

Не понимал, как менять текст и класс одновременно у переключателя состояния, использовал if (isOn), но забыл переключать сам флаг, поэтому кнопка работала только один раз, потом внёс isOn = !isOn внутрь обработчика, а с классами сначала прибавлял через +=, что не заменяло старый класс, поэтому перешёл на classList.add и classList.remove