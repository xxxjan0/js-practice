Сначала в счётчике забыл объявить переменную count глобально, из-за этого после обновления страницы счётчик сбрасывался, потом вынес за пределы обработчика

В live input не знал, что событие input срабатывает на каждое нажатие клавиши, а не только при потере фокуса, это оказалось удобно

Форма регистрации: долго не мог отменить отправку, забыл про preventDefault, форма перезагружала страницу

TODO список: при добавлении новой задачи не очищал поле ввода, приходилось удалять вручную, потом добавил taskInput.value = ''

Делегирование событий: сначала вешал обработчик на каждую кнопку, но при удалении карточки обработчики пропадали, потом использовал один обработчик на контейнер через event.target и closest

Фильтр каталога: не понимал, как обновлять список при изменении любого фильтра, сделал общую функцию filterAndSort и повесил её на input и change

Корзина с событиями: самая сложная задача, путался в обновлении количества и перерисовке корзины, использовал отдельные функции для добавления, удаления и изменения количества, пришлось несколько раз переписывать логику