Сначала хотел вывести каталог товаров из массива через обычный for, но потом узнал про forEach, однако при повторном нажатии кнопки товары дублировались, потому что я не очищал контейнер перед циклом, и добавил container.innerHTML = пустая строка в начало обработчика

При фильтрации товаров по названию не учёл регистр букв, из-за чего поиск Мышь не находил мышь в массиве, добавил приведение к нижнему регистру через toLowerCase() и для строки поиска, и для названий товаров

Использовал find для поиска пользователя по id, но забыл преобразовать строку из input в число, из-за этого id из инпута был строкой, а в объекте числом, и условие никогда не срабатывало, поэтому добавил parseInt() перед сравнением

При преобразовании цен через map сначала думал, что этот метод изменяет исходный массив, но он возвращает новый, это хорошо, а при форматировании строки вида Цена товара: 500 → со скидкой: 450 долго не мог понять, как получить индекс текущего элемента, пока не вспомнил, что второй параметр в map как раз и есть индекс

В задаче со списком задач при переключении между всеми, выполненными и невыполненными забывал обновлять отображаемый массив, поэтому сделал отдельную функцию render, которая принимает параметр фильтра и каждый раз заново фильтрует исходный массив, а не использует закешированный результат

При подсчёте общей суммы корзины сначала использовал цикл for, потом переделал на reduce, но забыл указать начальное значение, из-за чего получал NaN, когда массив был не пустой, а для поиска самого дорогого товара сначала применил Math.max только к ценам, но нужно было вывести ещё и название, поэтому переписал на reduce с сравнением объектов

Сортировка товаров по цене мутировала исходный массив, что запрещено условием, поэтому перед каждой сортировкой я создавал копию через спред-оператор [...products] и уже её сортировал, а ещё перепутал порядок: a.price - b.price — это возрастание, а b.price - a.price — убывание, проверил на своих данных